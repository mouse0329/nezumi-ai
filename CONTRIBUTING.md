# Contributing to Nezumi AI

まずは興味を持っていただきありがとうございます。このプロジェクトは現在ほぼ一人（mouse0329）で開発していて、コントリビューションを受け入れる体制がまだ整っていません。このドキュメントは「どこから手伝えるか」を明確にすることを目的にしています。

## 前提として知っておいてほしいこと

- 開発者は日本語話者です。Issue / PR は日本語・英語どちらでも歓迎します。
- レビューの返信には数日〜数週間かかることがあります（一人で全機能を見ているため）。
- コア推論エンジン（GGUF / LiteRT-LM / Nezumi Kiln）まわりは特に変更の影響範囲が大きいため、大きな改修は事前に Issue で相談してから着手してもらえると助かります。

## 参加しやすいところ

いきなりコア機能に手を出すより、以下から始めるのがおすすめです。

- **バグ報告**：再現手順、端末機種、Android バージョン、ログ（`adb logcat` の該当部分）があると非常に助かります。
- **UI文言・翻訳**：`app/src/main/res/values/strings.xml`（日本語）と `values-en/strings.xml`（英語）。訳が不自然な箇所の修正や、他言語の追加。
- **ドキュメント整備**：`docs/` 配下の誤字修正、手順の補足、実際にビルドして詰まった点の追記。
- **ビルド手順の検証**：特に Windows 以外（macOS / Linux）でのビルドを試して結果を共有してもらえると助かります。現状 Windows でのビルドしか動作確認できていません。

## 手を出す前に相談してほしいところ

- 推論エンジン (`data/inference/` 配下) の変更
- `mnn-sd-engine`（Nezumi Kiln）本体のロジック変更
- ライセンスに関わる外部依存の追加（[LICENSING.md](./docs/LICENSING.md) を参照）
- MCP / Mini App SDK のパーミッションモデルに関わる変更（セキュリティに直結するため）

## 開発環境のセットアップ

README.md の「開発環境」セクションを参照してください。

ビルド時の注意点（サブモジュール初期化忘れ、画像生成機能が別ビルドになっている点、Vulkan SDKまわりのハマりどころなど）は [docs/ANDROID_NATIVE_BUILD.md](./docs/ANDROID_NATIVE_BUILD.md) にまとめています。ビルド関連で詰まったら、まずここを確認してください。

それでも解決しない場合は Issue（Build Issue テンプレート）を立ててください。同じところで詰まる人は他にもいるはずなので、解決した内容をドキュメントに反映してもらえると次の人の助けになります。

## Pull Request の流れ

1. 変更内容が前述の「相談してほしいところ」に該当する場合は、先に Issue を立てて方向性を確認してください。
2. 小さい PR を歓迎します。1 PR = 1 目的を意識してもらえると助かります。
3. 既存のコードスタイル（Kotlin: 標準的な Android Kotlin Style、C++: 既存ファイルに合わせる）に合わせてください。
4. コミットメッセージは日本語・英語どちらでも構いません。

## ライセンスについて

このプロジェクトは LGPL v3 と商用ライセンスのデュアル構成です。PR を送ることで、あなたの変更が同じライセンス条件で配布されることに同意したものとみなします。この点に懸念がある場合は事前に Issue で相談してください。

## 行動規範

[CODE_OF_CONDUCT.md](./.github/CODE_OF_CONDUCT.md) を参照してください。
